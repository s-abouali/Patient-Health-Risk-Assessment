# 🩺 Patient Health Risk Assessment System

Python program that collects basic health information from a user and generates a simple health risk report. It calculates Body Mass Index (BMI), evaluates several common health factors, and classifies the user's overall risk level based on a scoring system.
